function boolToWord(bool) {
  return bool ? "Yes" : "No";
}
// The function boolToWord takes a single argument bool, 
// which is a boolean value (either true or false). 
// The function uses a ternary operator to determine what string to return based on the value of bool.
