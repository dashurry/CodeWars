const stringToNumber = function(str) {
  return parseInt(str);
}
// The parseInt function is a built-in JavaScript function that can be used to convert a string representation of a number (either an integer or a float) into an integer.
